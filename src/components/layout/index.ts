/**
 * Layout Components - Barrel Export
 * 
 * Re-exporta componentes de layout desde su ubicación actual.
 * Para nuevos componentes de layout, crearlos directamente en esta carpeta.
 * 
 * @example
 * import { Layout, AppSidebar } from "@/components/layout";
 */

export { Layout } from "../Layout";
export { AppSidebar } from "../AppSidebar";
